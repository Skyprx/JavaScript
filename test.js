const re = /java/i;
const str = "JavaScript";
const res = re.test(str)
console.log(res);

// The test() method executes a search for a match between
// a regular expression and a specified string. The i flag
// in regular expression is used for case insensitivity.